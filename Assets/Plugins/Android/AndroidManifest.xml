<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.DreamForge.IdiotOdyssey">

    <!-- Quyền thật sự cần cho ads/network -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Quyền phục vụ quảng cáo (ID thiết bị quảng cáo) -->
    <uses-permission android:name="com.google.android.gms.ads.permission.AD_ID"/>

    <!-- Notification (Android 13+ cần runtime + khai báo manifest) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- XÓA quyền Exact Alarm nếu có SDK nào lỡ thêm -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove"/>

    <application>
        <!-- App ID AdMob của bạn -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1945244255127558~9822899831"/>

        <!-- Trì hoãn App Measurement init cho đến khi bạn chủ động (an toàn hơn về consent) -->
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="true"/>

        <activity
            android:name="com.unity3d.player.UnityPlayerActivity"
            android:theme="@style/UnityThemeSelector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true"/>
        </activity>
    </application>
</manifest>
